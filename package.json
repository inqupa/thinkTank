{
  "name": "vent_codebase_2",
  "version": "1.0.0",
  "description": "",
  "main": "subsystems/routers/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx wrangler dev subsystems/routers/index.js --local --persist-to ../wrangler-data",
    "db:list": "npx wrangler d1 execute vent_black --local --command='SELECT * FROM vents;' --persist-to ../wrangler-data",
    "deploy": "wrangler deploy",
    "format": "prettier --write \"**/*.{js,css,html}\"",
    "lint": "eslint \"**/*.js\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "eslint": "^10.2.1",
    "prettier": "^3.8.3"
  }
}
